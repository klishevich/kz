<%= form_for(@ttype) do |f| %>
  <% if @ttype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ttype.errors.count, "error") %> prohibited this tender type from being saved:</h2>

      <ul>
      <% @ttype.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
