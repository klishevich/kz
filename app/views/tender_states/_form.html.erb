<%= form_for(@tender_state) do |f| %>
  <% if @tender_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tender_state.errors.count, "error") %> prohibited this tender_state from being saved:</h2>

      <ul>
      <% @tender_state.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
